// Seed functionality has been moved to a separate package to avoid circular dependencies
// This file is kept for backwards compatibility but is not currently used

export async function seed() {
  console.log(
    'Seed functionality has been moved. Please use the appropriate seeding tool.'
  )
}
